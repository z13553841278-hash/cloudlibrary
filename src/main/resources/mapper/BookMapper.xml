<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zjq.java_ee_starter.mapper.BookMapper">
    <resultMap id="bookResultMap" type="Book">
        <id property="id" column="id" />
        <result property="title" column="title" />
        <result property="author" column="author" />
        <result property="isbn" column="isbn" />
        <result property="category" column="category" />
        <result property="description" column="description" />
        <result property="totalCopies" column="total_copies" />
        <result property="availableCopies" column="available_copies" />
        <result property="status" column="status" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <sql id="bookBaseColumns"> id, title, author, isbn, category, description, total_copies,
        available_copies, status, create_time, update_time </sql>

    <select id="findBooksByCondition" parameterType="Book" resultMap="bookResultMap"> SELECT <include
            refid="bookBaseColumns" /> FROM books <where>
            <if test="title != null and title != ''"> AND title LIKE CONCAT('%', #{title}, '%') </if>
            <if
                test="author != null and author != ''"> AND author LIKE CONCAT('%', #{author}, '%') </if>
            <if
                test="category != null and category != ''"> AND category = #{category} </if>
            <if
                test="status != null"> AND status = #{status} </if>
        </where> ORDER BY
        create_time DESC </select>
</mapper>